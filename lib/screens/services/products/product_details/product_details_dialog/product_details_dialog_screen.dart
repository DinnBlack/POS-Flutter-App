import 'package:flutter/material.dart';

class ProductDetailsDialogScreen extends StatelessWidget {
  const ProductDetailsDialogScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
